Git
Push 
pull
clone
Git - reventing chnages
reset
rebase
pull requeta
Git mergre conflitc
git ignore
merging pull request
-------------

wenr ever file has modifeid need to git add filename and then commmit wil work or git restor to discrad chnages)
The git init -->once u do wills e .git folder

command creates a new Git repository. It can be used to convert an existing, 
unversioned project to a Git repository or initialize a new, empty repository. Most other Git commands are not available outside of an initialized repository, 
so this is usually the first command you'll run in a new project.

to make file traceble in git repositoryu use command git add. or git add filename 
git status - The git status command displays the state of the working directory and the staging area. It lets you see which changes have been staged, which haven't, and which files aren't being tracked by Git. 
Status output does not show you any information regarding the committed project history.

(use "git rm --cached <file>..." to unstage)
----
git log 
 (use "git rm --cached <file>..." to unstage
--
Jekins webhook
jekn=ins pipeline
IUntegrating artifcatory with jenkins
Sonarcute integrtion with jenkis
